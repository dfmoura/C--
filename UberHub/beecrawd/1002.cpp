#include <bits/stdc++.h>
#include <math.h>
#include <stdio.h>

using namespace std;
int main()
{
    float n =3.14159, raio, y=2, area; 
    cin >> raio;
    n = 3.14159;
    area = n * (pow(raio,y));
    printf("A=%.4f\n", area);
}