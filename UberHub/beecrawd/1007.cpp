#include <bits/stdc++.h>
#include <math.h>
#include <stdio.h>
#include <iomanip>

using namespace std;
int main(){
    int a , b, c, d, dif; 
    cin >> a, cin >> b, cin >> c, cin >> d;
    dif = (a*b-c*d);
    cout << "DIFERENCA = " << dif << endl;
}